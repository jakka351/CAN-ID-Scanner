# SocketCAN Bash Scripts

### CAN-ID-Scanner
Scan for ECU Rx &amp; Tx CAN ID Addresses with socketcan.

To run script:  
`sudo ./id-scanner.sh` 

### UDS-Service-Scanner  
Scans for UDS Services.

To run script:  
`sudo ./uds-scanner.sh`  
